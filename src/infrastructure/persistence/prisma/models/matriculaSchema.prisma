model Matricula {
    id           Int        @id @default(autoincrement())
    estado       String
    fecha        DateTime   @default(now())
    estudiante   Estudiante @relation(fields: [estudianteId], references: [id])
    estudianteId Int
    cursos       Curso[]    @relation(references: [id])
}
